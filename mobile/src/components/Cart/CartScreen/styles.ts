import { StyleSheet } from 'react-native';
import spacing from '../../../theme/spacing';

const styles = StyleSheet.create({
  container: { alignItems: 'center', minHeight: '100%' },
  price: { marginTop: spacing(2), marginBottom: spacing(4) },
  cartCardContainer: { marginBottom: spacing(2) },
});

export default styles;